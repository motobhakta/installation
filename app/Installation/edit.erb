<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>Edit <%= @installation.a %></h1>
    <a href="<%= url_for :action => :show, :id => @installation.object %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
    <a href="<%= url_for :action => :delete, :id => @installation.object %>" class="ui-btn-right" data-icon="delete" data-direction="reverse">
      Delete
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :update %>">
      <input type="hidden" name="id" value="<%= @installation.object %>"/>
      
          <div data-role="fieldcontain">
            <label for="installation[a]" class="fieldLabel">A</label>
            <input type="text" id="installation[a]" name="installation[a]" value="<%= @installation.a %>" <%= placeholder( "A" ) %> />
          </div>
      
      <input type="submit" value="Update"/>
    </form>
  </div>

</div>
